<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>@ViewData["Title"]</title>
</head>
<body>
    <div>
        @RenderBody()
        @if (IsSectionDefined("Secure"))
        {
            @await RenderSectionAsync("Secure");
        }
    </div>
</body>
</html>
